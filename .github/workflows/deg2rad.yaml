name: Deg2rad workflow
on:
  push
jobs:
  pusher_checker:
    runs-on: ubuntu-latest
    steps:
      - 
        name: pull repo
        uses: actions/checkout@v3
      -
        name: testing
        run: |
          docker build -t fse1 -f Dockerfile .
          docker run -it fse1
          ./test.sh
